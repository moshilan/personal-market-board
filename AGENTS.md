# 项目协作约定

## 当前阶段

`01｜核心数据源与采集`已完成，`02｜数据模型、缓存与历史`尚未开始。当前仅允许维护项目结构、说明文档、工作日志和Git配置；未经用户明确要求，不添加新的业务代码、数据抓取、接口调用、页面、依赖或部署配置。

## 数据约束

- 不依赖付费API、付费服务器或付费订阅
- 行情数据必须保留来源、数据时间和采集时间
- 金价仅展示可靠的当日最新数据，不用估算或过期数据冒充当前行情
- 油价按最新有效调价公告判断
- 无法可靠获取的数据必须显示不可用，不伪造或静默回退为实时数据

## 迁移边界

`20260711-market-price-analyzer`和`PersonalProject`只作为迁移来源。除非用户明确要求，不删除、不移动、不修改，也不整包复制。

